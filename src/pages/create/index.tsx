import { InputForm } from '@/components/InputForm/InputForm';
import { Title } from '@/components/Title/Title';

export default function CreatePage() {
  return (
    <section>
      <Title value="Создание продукта" />
      <InputForm />
    </section>
  );
}
